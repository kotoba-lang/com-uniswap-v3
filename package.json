{
  "private": true,
  "scripts": {
    "test": "kbb --backend sci --classpath src:test -e \"(require '[clojure.test :as t] 'uniswap_v3.main-test) (let [r (t/run-tests 'uniswap_v3.main-test)] (.exit js/process (if (pos? (+ (:fail r) (:error r))) 1 0)))\""
  }
}
